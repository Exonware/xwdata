# Large generated artifacts (keep repo clean)
/data/

# Local caches / indexes produced by demos
/operations/.cache/

# Safety: ignore common huge outputs even if user writes them elsewhere
*.jsonl
*.ndjson
*.idx.json
*.idx.jsonl
