"""chatdb_bigfile example operations.

This folder is intentionally a small, self-contained set of scripts that
exercise xwschema + xwdata + xwnode + xwquery + xwsystem JSONL serialization.

Run individual demos with:
  python xwdata/examples/chatdb_bigfile/operations/demo_paging.py

(When running from repo root, ensure the project is installed editable or that
PYTHONPATH includes the workspace root.)
"""
